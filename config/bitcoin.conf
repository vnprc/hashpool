server=1
rpcuser=username
rpcpassword=password
# Note: network (chain) is set from cmdline argument for convenience
# Note: sv2 template provision is handled by the sv2-tp binary (see config/sv2-tp.conf)
