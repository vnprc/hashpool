[network]
# Upstream pool connection (for translator and web-proxy display)
upstream_address = "127.0.0.1"
upstream_port = 34254
# Downstream miner connection
downstream_address = "80.71.235.186"
downstream_port = 34256

[mint]
host = "mint.hashpool.dev"
port = 443
url = "https://mint.hashpool.dev"

[pool]
port = 34254

[proxy]
port = 34255

[ehash]
# TODO: This should be retrieved dynamically from the mint
# Minimum difficulty (leading zero bits) required to earn 1 unit of ehash
minimum_difficulty = 32

[faucet]
enabled = true
host = "127.0.0.1"
port = 8083
faucet_timeout = 3

[web_proxy]
# Query step for Prometheus/VictoriaMetrics range queries (seconds)
metrics_query_step_secs = 15
# Interval in seconds for browser to poll web-proxy API
client_poll_interval_secs = 3
